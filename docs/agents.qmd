# AI Agents

An AI agent is an actor whose handler calls a language model. Nothing else changes.

```{=html}
<div class="two-col">
  <div>
    <div class="card"><h3>Tool calling</h3><p>Tools are actors. Calling a tool means publishing to its topic. The result arrives as a new message on a subscribed topic.</p></div>
    <div class="card"><h3>Multi-round state</h3><p>Conversation history lives in LMDB <code>state/</code>, keyed by correlation ID. Each round picks up exactly where the last left off.</p></div>
  </div>
  <div>
    <div class="card"><h3>Agent chaining</h3><p>An agent publishes to another agent's topic. Supervisor, specialist, critic — all natural compositions on the same bus.</p></div>
    <div class="card"><h3>Fault isolation</h3><p>A crashing handler exits. The runtime survives. The next tuple spawns a fresh process. No shared heap, no corrupted state.</p></div>
  </div>
</div>
```

## The ReAct Loop

ReAct — Reason + Act — is the standard agentic pattern: reason about what to do, call a tool, observe the result, iterate until done. On a mesh this loop is a **message flow**, not a control flow.

```{mermaid}
sequenceDiagram
  autonumber
  participant C  as Client
  participant AG as Agent actor
  participant TL as Tool actor

  C  ->> AG : user_message {query}
  Note over AG: load history from LMDB[corr_id], call LLM with tools
  AG ->> TL : sql_query {sql}
  Note over TL: execute SQL, return rows or error
  TL ->> AG : sql_result {rows} or {error}
  Note over AG: append tool result, call LLM again
  AG ->> C  : agent_response {answer}
  Note over AG: persist updated history to LMDB[corr_id]
```

The agent actor subscribes to both `user_message` and `sql_result`. The bus does not care that the same actor receives both. The `correlation_id` in the tuple header ties separate invocations into one logical conversation — and keys the LMDB memory that carries history across turns.

Duplicate delivery is handled atomically: the handler writes `ACTOR_TUPLE_ID` into a `dedup` LMDB database with `MDB_NOOVERWRITE`. If the key already exists the handler exits silently with no output, and the runtime publishes nothing.

::: {.callout}
**The loop has no fixed depth.** An agent can emit `sql_query`, receive `sql_result`, emit another `sql_query`, and so on up to whatever bound the handler enforces. The runtime is oblivious — it just delivers tuples.
:::

## Multi-Agent Topology

```{mermaid}
stateDiagram-v2
  direction TB

  User        : user
  Supervisor  : supervisor agent
  DataAgent   : data agent
  CodeAgent   : code agent
  WebAgent    : web agent
  SQLTool     : sql tool
  PyRunner    : python runner
  WebFetch    : web fetch

  [*]         --> User
  User        --> Supervisor  : request

  Supervisor  --> DataAgent   : data_query
  Supervisor  --> CodeAgent   : code_task
  Supervisor  --> WebAgent    : web_task

  DataAgent   --> SQLTool     : sql_query
  SQLTool     --> DataAgent   : sql_result

  CodeAgent   --> PyRunner    : run_python
  PyRunner    --> CodeAgent   : run_result

  WebAgent    --> WebFetch    : fetch_url
  WebFetch    --> WebAgent    : fetch_result

  DataAgent   --> Supervisor  : data_result
  CodeAgent   --> Supervisor  : code_result
  WebAgent    --> Supervisor  : web_result

  Supervisor  --> User        : response
  User        --> [*]
```

Each box is an independent process. The supervisor has no imports from specialist code. Every edge is a topic on the bus.

## PII Token Bubble

When the LLM endpoint is off-host (cloud API), raw PII — names, organisations — travels in plaintext. The token bubble ensures the LLM **never sees real PII** and **never returns real PII**.

```{mermaid}
sequenceDiagram
  autonumber
  participant U  as User
  participant TK as Tokenise
  participant AG as LLM Agent
  participant DK as Detokenise
  participant DB as SQLite Tool

  U  ->> TK : user_message {query}
  Note over TK: NER masking, write token map to LMDB[corr_id]
  TK ->> AG : user_message_masked
  Note over AG: sees TOK_PERSON_0001 not real names
  AG ->> DK : sql_query_masked
  Note over DK: detokenise SQL, restore real names
  DK ->> DB : sql_query
  DB ->> TK : sql_result {real employee data}
  Note over TK: mask all PII in result rows
  TK ->> AG : sql_result_masked
  Note over AG: sees TOK tokens in rows
  AG ->> DK : agent_response_masked
  Note over DK: restore tokens → real names
  DK ->> U  : agent_response
```

**Rule:** everything the LLM sees is tokenised. Everything the LLM emits is detokenised. SQL and user I/O live outside the bubble.

| Topic | Direction | Contains |
|-------|-----------|----------|
| `user_message_masked` | Tokenise → LLM | Query with entity tokens |
| `sql_result_masked` | Tokenise → LLM | DB rows with PII tokens |
| `sql_query_masked` | LLM → Detokenise | SQL with entity tokens |
| `agent_response_masked` | LLM → Detokenise | Answer with PII tokens |
| `sql_query` | Detokenise → SQLite | Real SQL with real names |
| `agent_response` | Detokenise → Client | Clean text |

The `correlation_id` in the tuple header is the shared key — tokenise and detokenise use it to read/write the same LMDB token map, even though they are separate processes.

### Token Format

Entities are mapped to label-aware tokens like `TOK_PERSON_0001`, `TOK_ORG_0000`. The label gives the LLM entity-type context without revealing the value. The system prompt instructs the LLM to pass tokens through **verbatim** — never modify, truncate, or interpret them.

### ZMQ Subscription Guard

Topics are designed to prevent prefix-match loops: `user_message` and `user_message_masked` differ at the 13th byte (null vs underscore). Subscriptions include the null terminator (`strlen(topic) + 1`) to ensure exact matching, never prefix.

### Handler

A single C binary (`handlers/pii-filter.c`) serves both mask and restore roles via `PII_MODE=mask|restore`. Mask mode runs ONNX NER inference (spaCy CNN, 6 MB model), replaces entity spans with tokens, and writes mappings to LMDB. Restore mode scans for token patterns, looks up originals in LMDB, and substitutes them back. Both modes walk MessagePack documents recursively — strings at any nesting depth are processed transparently.
